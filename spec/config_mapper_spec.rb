require 'spec_helper'

describe ConfigMapper do
  it 'has a version number' do
    expect(ConfigMapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
